node_modules/
dist/
.env
.env.postgres.local
brain.projects.json
*.log
.DS_Store
*.tsbuildinfo
.playwright-cli/

# Preserve ignored local data from the retired delivery tools.
analytics/baseline/config.json
analytics/baseline/out/*
analytics/timelog/timelog.csv
# Runtime data dir: SQLite history, cost ledger, and encrypted credentials.
# Ignore the whole directory - never enumerate extensions here, or a new
# runtime file silently becomes committable.
server/data/
