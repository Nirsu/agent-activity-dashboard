# Shared repository instructions

@AGENTS.md
@BRAIN-MCP.md

Use the shared Brain workflow automatically for implementation inside a
registered project's allowed scope. Use the `harmony-brain` MCP configured for
this machine; never infer that a completed analysis is permission to merge.
